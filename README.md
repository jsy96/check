# 509simu · 相机仿真参数工具集

## 更新记录

- **2026-08-26** `wiskbroom.html` v1.0.260826-12：时序约束去提示——「摆扫周期 ≥ 成像时长」在 T &gt; T成像（存在往返时间）时由 ⚠ info 提示改为 ✓ ok（T = T成像 + T往返 是正常摆扫时序，有回扫时间不再黄标；仅 T &lt; T成像 的真时序矛盾报红），detail 显示成像 + 往返分解。test_core.cjs Case H 增加回归断言，9 个用例全部通过。
- **2026-08-26** `wiskbroom.html` v1.0.260826-11：不漏扫判据速度口径定稿——「每次摆扫距离不漏扫」中的 v 明确为**卫星相对地表速度（星下点地速 v_g = v·R/a）**，不再是惯性系轨道速度 v（判据由 T·v ≤ D∥ 改为 T·v_g ≤ D∥，更准确）；约束名、参考量、公式说明同步。Case B/H 断言数值更新（v_g·T），9 个用例全部通过。
- **2026-08-26** `wiskbroom.html` v1.0.260826-10：去重——删除与「每次摆扫距离不漏扫（T·v ≤ D∥）」判据相同、仅速度取值不同的「条带间不漏扫（v_g·T ≤ D∥）」（两者对比对象均为 D∥，保留老金定稿的卫星速度口径），物理约束由 5 条减为 4 条；参考量「摆扫周期内卫星飞行距离 = v_g·T」改为「每次摆扫距离 = T×卫星速度」。test_core.cjs Case B 改验证每次摆扫距离判据（T=8 s 触发）、Case H 改为判据通过侧边界用例（T=6.5 s ok），9 个用例全部通过。
- **2026-08-26** `wiskbroom.html` v1.0.260826-9：**摆扫方向瞬时视场角定义定稿 = atan(M·p⊥/f)**（整阵列一次成像的瞬时视场，M 为穿迹探测器个数、W = M·p⊥），约束② 按此计算（等价于瞬时地面覆盖 D⊥ > 帧间距离 Δθ×H 的覆盖口径）；参考量新增「摆扫方向瞬时视场角 = atan(M·p⊥/f)」，原「摆扫方向分辨率 GSD⊥」更名回「星下点 GSD（穿迹，单像元）」避免术语混淆（瞬时视场角 = 阵列口径，单像元 IFOV = p⊥/f 另列）。9 个用例全部通过。
- **2026-08-26** `wiskbroom.html` v1.0.260826-8：帧间判据改为角度口径——约束② 更名「**摆扫方向瞬时视场角 > 帧间角度**」（IFOV⊥ = p⊥/f 直接与 Δθ 对比，等价于原地面距离版 IFOV⊥×H > Δθ×H；地面折合值作补充显示，且不再依赖 H 参与判据）；参考量「摆扫方向帧间距离折合像元数」简化为「帧间步进折合像元数（Δθ / IFOV⊥）」。test_core.cjs Case F 匹配新名称（IFOV⊥ = 10 μrad < Δθ = 0.6° 触发），9 个用例全部通过。
- **2026-08-26** `wiskbroom.html` v1.0.260826-7：沿迹组「摆扫周期T」下新增参数「**一个摆扫周期内成像时长 T成像**」（= N_f×t_f 推导，也可由 T − T往返 反推；T 亦可由 T成像 + T往返 组合反推，恒等式参与互检）；原「参考量」中的成像时长行移除（升为参数后在推导汇总/输入栏展示）。test_core.cjs Case C/I 增加 T成像 断言，9 个用例全部通过。
- **2026-08-26** `wiskbroom.html` v1.0.260826-6：参数名精确化——沿迹组参数「摆扫周期 T」改为「摆扫周期T」（无空格；「卫星飞行时长 = 」前缀已于 v1.0.260826-3 移除，页面文案/公式说明同步统一写法）。
- **2026-08-26** `wiskbroom.html` v1.0.260826-5：帧间判据术语定稿——**摆扫方向分辨率 = IFOV⊥×H（= H·p⊥/f），摆扫方向帧间距离 = Δθ×H；分辨率 > 帧间距离才无漏缝**，约束② 更名「帧间无漏缝（摆扫方向分辨率 ≥ 帧间距离）」，bad 分支明确报「帧间漏缝（欠采样）」；参考量「星下点 GSD（穿迹）」更名「摆扫方向分辨率 GSD⊥ = IFOV⊥×H」，「帧间步进折合像元数」同步更名「摆扫方向帧间距离折合像元数」。test_core.cjs Case F 匹配新名称，9 个用例全部通过。
- **2026-08-26** `wiskbroom.html` v1.0.260826-4：不漏扫判据定稿——**T×速度 < 沿轨方向视场大小对应的地面距离 D∥（= H·Ω∥ = H·L/f）**，① 地速口径（v_g·T ≤ D∥）/ ⑤ 卫星速度口径（T·v ≤ D∥）两条约束同步回改（上一版把对比右端误扩为 v·T成像 + D∥）；该判据为保守充分条件（隐含往返期间推进 < D∥）。T = T成像 + T往返 的周期口径、参数与 noCheck 机制保持不变。test_core.cjs Case B/H 触发值回改（T=8 s / 7 s），9 个用例全部通过。
- **2026-08-26** `wiskbroom.html` v1.0.260826-3：摆扫周期口径修正——T = 成像时长 T成像（N_f×t_f）+ 往返时间 T往返（回扫，不成像）；参数「卫星飞行时长 = 摆扫周期 T」更名「摆扫周期 T」，新增参数 T往返（= T − N_f·t_f 推导，也可作为输入由 T = N_f·t_f + T往返 反推 T）；「T = N_f·t_f（连续摆扫）」标记为假设型公式（noCheck）不再参与恒等式互检，避免输入带往返时间的 T 报假矛盾。test_core.cjs：Case C 增加 T往返=0 断言，新增 Case I（T往返 反推 T），共 9 个用例全部通过。
- **2026-08-26** `wiskbroom.html` v1.0.260826-2：物理约束第 ② 项由「帧间不漏扫（s ≤ D⊥）」改为「帧间步进 ≤ 摆扫方向地面分辨率（H·Δθ ≤ GSD⊥）」——帧间角度 Δθ 对应地面步进与穿迹单像元 GSD（H·p⊥/f）对比，超过即帧间欠采样报警，不再检测帧间漏扫；参考量「帧间重叠率（穿迹）」同步改为「帧间步进折合像元数（H·Δθ / GSD⊥）」。test_core.cjs 基准夹具镜转速改为 4e-4 rad/s（Δθ 步进 4 m = 0.8 像元 ≤ GSD⊥ 5 m），Case F 改验证欠采样报警（8 个用例全部通过）。
- **2026-08-26** `wiskbroom.html` v1.0.260826-1：物理约束第 ⑤ 项由「摆扫边缘帧间不漏扫（球面）」（边缘球面步进 vs 边缘球面瞬时覆盖）改为「每次摆扫距离不漏扫（T·v ≤ D∥）」——每次摆扫距离 = 摆扫周期 T × 卫星轨道速度，与沿迹地面覆盖 D∥ 对比（较地速口径 v_g·T 更保守）；边缘球面步进/瞬时覆盖仍保留在「参考量」中。test_core.cjs 新增 Case H（8 个用例，全部通过）。

单文件网页工具（双击用浏览器打开即用，无任何依赖）+ Node 自测脚本：

| 文件 | 说明 |
|---|---|
| `index.html` | 主页「爱玩的老人」（v1.0.260820-6，视觉美化版：极光渐变背景、玻璃吸顶栏、渐变图标卡片与悬停/入场动效）：上半部分「本站工具与游戏」依次链接 pushbroom.html / wiskbroom.html / game.html（一起小学在最右侧），下半部分「有趣的声音」链接 bigdog.leige.site / music.leige.site |
| `wiskbroom.html` | 面阵相机卫星摆扫 · 参数一致性校验（v1.0.260826-12） |
| `pushbroom.html` | 线阵推扫相机参数计算（v1.0.260820-5） |
| `test_core.cjs` | wiskbroom.html 核心计算自测 |
| `test_pushbroom.cjs` | pushbroom.html 核心计算自测（含用 taijing304_MSS.coef 复现几何模型） |
| `taijing304_MSS.coef` | 线阵推扫相机安装系数实例文件（4 波段 × 3 片 × 3072 探元），推扫工具的输出格式参照 |
| `game.html` | 独立娱乐页面（v1.0.260820-1） |

全站所有页面页脚均悬挂备案号「鄂ICP备2025092414号-4」，链接至工信部备案系统。

---

## 部署（阿里云 ECS · Ubuntu 24.04）

全站为纯静态文件，由 nginx 托管，站点根目录 `/var/www/html/`，公网地址 `http://101.132.36.207/`。更新页面后把对应文件重新上传覆盖即可（如 `scp index.html root@<IP>:/var/www/html/`）。

---

## 工具一：面阵相机卫星摆扫 · 参数一致性校验（wiskbroom.html）

对摆扫（whisk-broom）成像模式的面阵相机卫星做粗略几何/时序计算：输入焦距、轨道、探测器、帧频、摆扫等参数，工具自动推导缺失量、并对所有冗余给出方式做**矛盾检测**（含漏扫判据）。

**使用**：左栏按组输入（全局/轨道、沿迹、穿迹），留空的参数自动由公式推导（推导值显示在输入框下方）；右栏实时输出推导汇总、恒等式一致性校验、物理约束（帧间无漏缝、每次摆扫距离不漏扫、时序、地平线）、参考量。容差（默认 2%）可调；输入自动保存到浏览器 localStorage；「保存/读取参数 TXT」为参数存档（UTF-8 带 BOM，行格式 `参数ID = 数值 [单位]`，`#` 后为注释）。

**口径**：内部统一 SI 单位；45° 扫描镜 2:1（视线角速度 = 2×镜转速）；总摆扫角 Θ = N_f×Δθ；摆扫周期 **T = 成像时长 T成像（N_f×t_f）+ 往返时间 T往返**（回扫，不成像，可输入反推；T往返 &gt; 0 属正常时序，不提示）；帧间判据 `摆扫方向瞬时视场角 > 帧间角度 Δθ`（瞬时视场角 = atan(M·p⊥/f)，整阵列一次成像的瞬时视场；等价于瞬时地面覆盖 D⊥ > 帧间距离 Δθ×H，大于才无漏缝，反之报帧间漏缝/欠扫描）；不漏扫判据 `T×v_g ≤ D∥`（v_g 为卫星相对地表速度 = 星下点地速 = v·R/a；D∥ = 沿轨方向视场大小对应的地面距离 = H·L/f，保守充分条件）；地心角/幅宽用球面换算，其余为星下点平面近似。公式细节见页面底部「公式与口径说明」。

**自测**：`node test_core.cjs`（9 个用例：推导链闭环、漏扫触发、矛盾报警、每次摆扫距离判据、T往返反推 T、TXT 往返等）。

---

## 工具二：线阵推扫相机参数计算（pushbroom.html）

输入线阵推扫相机参数，实时计算并**生成/保存 `taijing304_MSS.coef` 格式的安装系数文件**到本地。

### 输入参数

- **探测器/格式**：波段数 B、每波段 CCD 片数 N、每片探元数 W、拼接重叠像元数 ovl（默认 0）、多项式阶数 nCamPoly（默认 5 → 每行 6 个系数）、文件块顺序（片优先/波段优先）、虚拟片 X 取法、输出文件名；
- **轨道/光学**：焦距 f、探元尺寸 p、轨道高度 H、GSD——恒等式 `GSD = p·H/f`（星下点平面近似），任留空一项自动推导；地球半径 R（预填 6371 km，供地平线角校验）；
- **CCD 排列（沿轨方向 X）**：各波段均有沿轨**基偏移**（按"焦面距离 d（tanX = d/f）"或"离轴角 α（tanX = tanα）"输入，可切换、自动换算，支持 d₁+Δd 等间距快速填充 + 每波段单独修改）：
  - **共线排列**：tanX(片i, 波段b) = 波段 b 基偏移；
  - **交错排列**：tanX(片i, 波段b) = 波段 b 基偏移 + 奇/偶片沿轨交错分量（奇、偶分量各一值，各波段共用）。

### 输出与校验

- 右栏实时 **.coef 文件预览**（即文件字节内容）与「保存 .coef 文件」按钮；
- 推导汇总：像元角步长 p/f、虚拟片宽、全视场角、幅宽（= 虚拟片宽×GSD）、边缘离轴角/边缘穿轨采样等；
- 一致性校验：GSD 恒等式（超容差报红）、ovl 范围、奇偶错开/波段错开、边缘离轴角 ≤ 地平线角等；
- **焦面布置示意图**：各片 CCD 在焦面上的位置关系（x 沿轨向下为正、y 垂轨向左为正，探元 0 在各片左端；垂轨真实比例、沿轨行间距示意），按波段着色，悬停显示数值；
- 布置明细：各波段 tanX/离轴角/沿轨地面基线（配准用，交错排列含奇/偶片总 tanX），各片 det0/末探元 tanY 与垂轨角范围；
- 输入自动保存 localStorage；「保存/读取参数 TXT」参数存档（同工具一风格，元参数行 `layout / offsetMode / blockOrder / tolerance / filename`，`dbN` 为第 N 波段偏移）。

### .coef 文件格式（与 taijing304_MSS.coef 一致）

ASCII、**CRLF 换行、无 BOM**、末尾单个换行；系数 C 风格 `%.18e`（指数至少两位，如 `e-02`）；`angleBias*` 三行 `%.18f` 定点。

```
nCamPoly=5                 多项式阶数（每行系数个数 = 阶数+1）
bandCount=4                波段数 B
sensorCount=3              每波段 CCD 片数 N
angleBiasRoll=0.000...     零偏置（%.18f）
angleBiasPitch / angleBiasYaw 同上
angleBiasR:1.0e+00 ...     3×3 行优先单位阵（9 个 %.18e，冒号后无空格）
随后 1 + N×B 个探元块，每块 3 行：
width= 9166                探元数（"width=" 后有一个空格）
lookAngleX: c0 c1 …        沿轨方向 tan 多项式（自变量为 0 基探元序号 j）
lookAngleY: c0 c1 …        垂轨方向 tan 多项式（"lookAngleY:" 后有一个空格）
```

- 系数含义：多项式取值 (tanX, tanY) 使 **`(tanX, tanY, 1)` 为相机坐标系探元视线矢量**；
- **块顺序（片优先）**：虚拟片 → 第 1 片的第 1…B 波段 → 第 2 片的第 1…B 波段 → …（与需求说明及实例文件一致；也可切换为波段优先）；
- **虚拟片（第 0 块）**：沿轨最前（图上最上方）那组 CCD 拼接得到的虚拟 CCD，width = N·W−(N−1)·ovl；lookAngleX 默认取**全局最小**（与 taijing304_MSS.coef 实例一致）：共线 = tanX 最小的波段，交错 = 最小基偏移 + 较小交错分量；备选「片内最大→片间最小」= 最大基偏移 + 较小分量；
- **几何模型**（已用实例文件数值复现，最大相对偏差 1.4e-16）：像元角步长 s = p/f；整线关于主点垂轨居中，半跨度 = 虚拟片宽×s/2；第 i 片 lookAngleY 常数项 = 半跨度 − (i−1)·(W−ovl)·s、一次项 = −s；lookAngleX 常数项 = tan(波段/奇偶片沿轨离轴角)、高次项为 0；
- 实例文件参数（反推）：p/f = 3.995861429234007983e-06、ovl = 25 像元、虚拟片宽 9166。

### 自测

```
node test_pushbroom.cjs
```

8 个用例：实例文件几何复现（13 块全部系数）、片/波段块顺序、垂轨布置居中、GSD 闭包与矛盾报警、交错排列、非法参数拦截、%.18e/%.18f 与 CRLF 字节格式、参数 TXT 往返（含角度单位、BOM、dbN 行）。
